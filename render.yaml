services:
  - type: web
    name: work-schedule-bot
    runtime: python
    plan: free

    buildCommand: pip install -r requirements.txt

    startCommand: python bot.py

    envVars:
      - key: BOT_TOKEN
        sync: false

      - key: WEBHOOK_SECRET
        value: work_schedule_secret

    pythonVersion: 3.12.10